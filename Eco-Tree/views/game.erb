<!DOCTYPE html>
<html>
  <head>
    <title>Eco-Tree</title>
    <link rel="stylesheet" href="/styles/game.css" />
    <link rel="icon" href="/front/icon.png"/>
  </head>
  <body>
    <header>
      <div class="profile-link">
          <a href="/account">Mi Perfil</a>
      </div>
    </header>
    <div class="play-section">
      <h2>Vamos a jugar!!</h2>
      <h3>Pregunta <%=@question.id%>:</h3>
      <p><%= @question.description %></p>
      <form action="/game" method="POST">
        <div class="options">
          <% @options.each do |option| %>
            <label class="option">
              <input type="radio" name="selected_option_id" class="option-button" value="<%= option.id %>">
              <%= option.description %>
            </label>
          <% end %>
        </div>
        <input type="submit" value="Responder" class="option-button">
      </form>

  </body>
</html>