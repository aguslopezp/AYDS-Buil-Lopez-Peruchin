<!DOCTYPE html>
<html>
  <head>
    <title>Eco-Tree</title>
    <link rel="stylesheet" href="/styles/game.css" />
    <link rel="icon" href="/front/icon.png"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300&display=swap" rel="stylesheet">
  </head>
  <body>
    <header>
      <nav class="navigation">
          <a href="/account" class="profile">Mi Perfil</a>
      </nav>
    </header>
    <section class="play-section">
      <h2>Vamos a jugar!!</h2>
      <h3>Pregunta <%=@question.id%>:</h3>
      <p><%= @question.description %></p>
      <form action="/game" method="POST">
        <div class="options">
          <% @options.each do |option| %>
            <label class="option">
              <input type="radio" name="selected_option_id" class="option-button" value="<%= option.id %>">
              <%= option.description %>
            </label>
          <% end %>
        </div>
        <input type="submit" value="Responder" class="submit-button">
      </form>
    </section>
  </body>
</html>